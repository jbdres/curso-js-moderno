/** Crear Arrays en JavaScript */
// Sirve para agrupar elementos del mismo tipo
const numeros = [10, 20, 30];
console.log(numeros);

const meses = new Array('Enero', 'Febrero', 'Marzo');
console.log(meses);

// Arreglo que contiene datos de todo tipo
const deTodoTipo = ['Hola', 12, 52, null, true, {nombre:'Julian', trabajo:'Programador'}, [1,2,3]];
console.log(deTodoTipo);