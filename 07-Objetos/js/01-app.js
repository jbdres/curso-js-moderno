/* Crear Objetos en JavaScript * */
const nombre = "Monitor de 20 pulgadas";
const precio = 300;
const disponible = true;

// Un objeto agrupa todos los datos en una sola variable

// Object literal 
const producto = { // Separamos los atributos con comas
    nombre: "Monitor de 20 pulgadas", // llave y valor
    precio: 300,
    disponible: true
}; 

console.log(producto);